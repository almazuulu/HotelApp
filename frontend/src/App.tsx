// Placeholder shell that proves the dev server and hot reload work inside Docker.
// The router, providers, design system and feature modules arrive with the
// frontend scaffolding issue; src/app, src/features, src/pages, src/shared,
// src/assets and src/styles are created there.
export function App() {
  return (
    <main>
      <h1>HotelApp</h1>
      <p>Локальное окружение запущено. Интерфейс приложения пока не реализован.</p>
    </main>
  )
}
